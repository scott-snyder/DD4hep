Bottom: 08230debc682b33198c29ff7644db3a44e7d5164
Top:    b0f97d02e72bb2c2a45090770d5408ee0370549c
Author: sss <sss@karma>
Date:   2026-04-13 11:57:50 -0400

Fix compilation with C++23.

P2321R2 adds a pair<> constructor taking a non-const reference.
In that case, when we contruct a pair involving OpaqueDataBlock
from a non-const pair, then the move constructor can be called
instead of the copy constructor, with OBJECT=OpaqueDataBlock&.
Since that's a reference type, the new expression then won't compile.
Fix by adding an OpaqueDataBlock ctor taking a non-const reference.

Symptom is these warnings:

```
/home/sss/fcc/soft/DD4hep/DDCore/include/DD4hep/OpaqueData.h: In member function
 ‘T& dd4hep::OpaqueDataBlock::bind(T&&) [with T = dd4hep::OpaqueDataBlock]’:
/home/sss/fcc/soft/DD4hep/DDCore/include/DD4hep/OpaqueData.h:208:35: warning: infinite recursion detected [-Winfinite-recursion]
  208 |   template <typename T> inline T& OpaqueDataBlock::bind(T&& obj)   {
      |                                   ^~~~~~~~~~~~~~~
In constructor ‘dd4hep::OpaqueDataBlock::OpaqueDataBlock(OBJECT&&) [with OBJECT = dd4hep::OpaqueDataBlock]’,
    inlined from ‘T& dd4hep::OpaqueDataBlock::bind(T&&) [with T = dd4hep::OpaqueDataBlock]’ at /home/sss/fcc/soft/DD4hep/DDCore/include/DD4hep/OpaqueData.h:210:14:
/home/sss/fcc/soft/DD4hep/DDCore/include/DD4hep/OpaqueData.h:188:9: note: recursive call
  188 |     bind(std::move(obj));
      |     ~~~~^~~~~~~~~~~~~~~~
```
