Bottom: ab72983421f200962092d6357dd6b26c354394fe
Top:    004dcbb17dac65b20278e113383ef2d43be7d478
Author: sss <sss@karma>
Date:   2025-05-07 18:25:51 -0400

Fix undefined behavior.

Referencing argv[0] is undefined if argv is empty.
Fixed.
Also clean up the previous line

```
      argv.emplace_back(&((*i)[0]));
```

to use data() instead:

```
      argv.emplace_back(i->data());
```

which is much more readable.
